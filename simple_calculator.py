#by default input is treated as a string so we must convert the string into numbers using int(), float() placing the variables inside the function
number_1= input("Enter a number: ")
number_2= input("Enter another number: ")
result = float(number_1) + float(number_2)

#by default input is treated as a string so we must convert the string into numbers
print(result)




































